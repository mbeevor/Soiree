<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>
        <!-- enable overlap of transitions -->
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
    </style>

    <!-- style for main heading text -->
    <style name="main_heading">
        <item name="android:fontFamily">serif</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorPrimaryText</item>
        <item name="android:textSize">@dimen/heading_text_size</item>
    </style>

    <!-- style for ingredients text -->
    <style name="ingredients_item">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@color/colorPrimaryText</item>
        <item name="android:textSize">@dimen/normal_text_size</item>
    </style>

    <!-- style for dinner party label -->
    <style name="dinner_party_label">
        <item name="android:fontFamily">serif</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/sub_heading_text_size</item>
    </style>

    <!-- style for rating  -->
    <style name="rating">
        <item name="android:fontFamily">serif</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/heading_text_size</item>
    </style>

    <!-- widget heading -->
    <style name="widget_heading" >
        <item name="android:textSize">28sp</item>
        <item name="android:padding">@dimen/narrow_padding</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="widget_list_item" >
        <item name="android:textSize">@dimen/normal_text_size</item>
        <item name="android:padding">@dimen/narrow_padding</item>
        <item name="android:layout_margin">@dimen/narrow_margin</item>
        <item name="android:textColor">@color/colorPrimaryText</item>
    </style>


</resources>
